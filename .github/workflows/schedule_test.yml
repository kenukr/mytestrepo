name: выполнение по времени

on:
  schedule:
    - cron:  '00 12 * * *'

jobs:
  test_schedule:
    runs-on: ubuntu-latest
    steps:
      - name: Every time
        run: echo "This step will always run"
